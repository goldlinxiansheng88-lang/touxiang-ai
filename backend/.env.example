# 本文件路径：backend/.env（与 uvicorn 启动目录无关，应用会固定从 backend/.env 与可选的仓库根 .env 加载）。
# 复制为 backend/.env 后填写。数据库与 Redis 请使用云托管或其它可访问实例（本仓库不再附带本地 Docker 数据库）。
#
# DATABASE_URL：云平台「Direct connection」整段 URI（postgresql://…）；服务端会自动改为 postgresql+psycopg://。
# REDIS_URL：云 Redis 或本机已安装的 Redis（redis:// 或 rediss://）。
# 连接 Supabase / Neon 等若未带 ssl 参数，服务端可能自动追加 sslmode=require。
#
# 若本机访问 /health/db 报 getaddrinfo failed / 无法解析主机：多为 DNS 或网络问题（与代码无关）。
# 可尝试：① 将 Windows 网卡 DNS 改为 8.8.8.8 / 1.1.1.1 后重试 ② 在 Supabase 控制台换用「Session pooler」连接串（主机常为 pooler 域名、端口 6543，部分网络下比 Direct 更易连通）。
# 自检：启动 API 后浏览器打开 http://127.0.0.1:8000/health/db ；另见 http://127.0.0.1:8000/health/env

DATABASE_URL=postgresql+psycopg://USER:PASSWORD@db.example.supabase.co:5432/postgres
REDIS_URL=rediss://default:PASSWORD@xxx.upstash.io:6379

# 可选：连接串里未写 ?sslmode=... 时强制指定
# DATABASE_SSLMODE=require

# Fernet 加密主密钥（32 url-safe base64 字节）
# 生成: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=

# ADMIN_PASSWORD=changeme
# PUBLIC_BASE_URL=http://localhost:8000
# FRONTEND_URL=http://localhost:5173

# 用户登录（邮箱 / Google / Microsoft）：PUBLIC_BASE_URL 必须与 OAuth 控制台里填的「授权重定向 URI」主机一致。
# 回调路径固定为：{PUBLIC_BASE_URL}/api/auth/oauth/google/callback 与 .../oauth/microsoft/callback
# JWT_SECRET=（可选，留空则用 ENCRYPTION_KEY 派生）
# GOOGLE_OAUTH_CLIENT_ID=
# GOOGLE_OAUTH_CLIENT_SECRET=
# MICROSOFT_OAUTH_CLIENT_ID=
# MICROSOFT_OAUTH_CLIENT_SECRET=

# GET /api/locale/detect：服务端根据访问者 IP 调用 ip-api.com 推断地区并返回推荐界面语言（需 API 能访问外网）。
# RUNTIME_LOG_FILE=logs/api.log
