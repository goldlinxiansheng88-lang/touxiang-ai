export type Scene = { id: string; label: string; icon: string; ratio: string };
export type StyleItem = {
  id: string;
  display_name: string;
  subtitle: string;
  social_proof: string;
  thumbnail_url: string;
};
